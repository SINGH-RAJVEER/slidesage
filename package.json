{
  "name": "slide-sage-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "latest"
  },
  "trustedDependencies": [],
  "packageManager": "bun@1.0.25"
}
